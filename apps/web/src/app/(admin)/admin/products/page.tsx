/**
 * Maison — Admin products page (stub — Phase 1)
 */

export default function AdminProductsPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Products
      </h2>
      <p style={{ color: "#8a8178" }}>
        Product table (search, filter, inline edit, bulk actions) — Phase 1.
      </p>
    </div>
  );
}
