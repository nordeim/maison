/**
 * Maison — Admin customers page (stub — Phase 1)
 */

export default function AdminCustomersPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Customers
      </h2>
      <p style={{ color: "#8a8178" }}>
        Customer directory (search, order history, total spend) — Phase 1.
      </p>
    </div>
  );
}
