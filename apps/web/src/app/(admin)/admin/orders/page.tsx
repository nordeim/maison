/**
 * Maison — Admin orders page (stub — Phase 1)
 */

export default function AdminOrdersPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Orders
      </h2>
      <p style={{ color: "#8a8178" }}>
        Order table (filter by status/date, fulfillment actions) — Phase 1.
      </p>
    </div>
  );
}
