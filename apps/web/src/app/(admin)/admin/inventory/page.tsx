/**
 * Maison — Admin inventory page (stub — Phase 1)
 */

export default function AdminInventoryPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Inventory
      </h2>
      <p style={{ color: "#8a8178" }}>
        Stock levels by SKU, low-stock alerts, bulk restock — Phase 1.
      </p>
    </div>
  );
}
