/**
 * Maison — Account dashboard (stub — Phase 1)
 */

export default function AccountDashboardPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Dashboard
      </h2>
      <p style={{ color: "#8a8178" }}>
        Welcome to your Maison account. Full dashboard (recent orders, wishlist count, saved
        addresses) — Phase 1.
      </p>
    </div>
  );
}
