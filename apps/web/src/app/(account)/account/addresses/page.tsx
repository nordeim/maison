/** Account addresses page (stub — Phase 1) */
export default function AccountAddressesPage() {
  return (
    <div>
      <h2 style={{ fontFamily: "Georgia, serif", fontSize: "1.5rem", fontWeight: 500, marginBottom: "1.5rem" }}>
        Saved Addresses
      </h2>
      <p style={{ color: "#8a8178" }}>Address CRUD with default shipping/billing selection — Phase 1.</p>
    </div>
  );
}
